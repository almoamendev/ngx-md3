/**
 * The position of the item row inside the toolbar container.
 *
 * This has a visible effect on a docked toolbar only. A floating container is as wide as its
 * content, so it has no free space to align in.
 */
export type ToolbarAlignment = 'start' | 'center' | 'end';
