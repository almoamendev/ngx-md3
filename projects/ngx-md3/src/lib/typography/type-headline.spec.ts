import { TypeHeadline } from './type-headline';

describe('TypeHeadline', () => {
  it('should create an instance', () => {
    const directive = new TypeHeadline();
    expect(directive).toBeTruthy();
  });
});
