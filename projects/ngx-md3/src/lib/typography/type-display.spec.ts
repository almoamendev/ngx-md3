import { TypeDisplay } from './type-display';

describe('TypeDisplay', () => {
  it('should create an instance', () => {
    const directive = new TypeDisplay();
    expect(directive).toBeTruthy();
  });
});
