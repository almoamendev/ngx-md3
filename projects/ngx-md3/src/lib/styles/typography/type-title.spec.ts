import { TypeTitle } from './type-title';

describe('TypeTitle', () => {
  it('should create an instance', () => {
    const directive = new TypeTitle();
    expect(directive).toBeTruthy();
  });
});
