import { ScaffoldRail } from './scaffold-rail';

describe('ScaffoldRail', () => {
  it('should create an instance', () => {
    const directive = new ScaffoldRail();
    expect(directive).toBeTruthy();
  });
});
