import { ScaffoldBar } from './scaffold-bar';

describe('ScaffoldBar', () => {
  it('should create an instance', () => {
    const directive = new ScaffoldBar();
    expect(directive).toBeTruthy();
  });
});
