import { ScaffoldPane } from './scaffold-pane';

describe('ScaffoldPane', () => {
  it('should create an instance', () => {
    const directive = new ScaffoldPane();
    expect(directive).toBeTruthy();
  });
});
