import { SupportingText } from './supporting-text';

describe('SupportingText', () => {
  it('should create an instance', () => {
    const directive = new SupportingText();
    expect(directive).toBeTruthy();
  });
});
