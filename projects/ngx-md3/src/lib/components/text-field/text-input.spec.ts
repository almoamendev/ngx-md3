import { TextInput } from './text-input';

describe('TextInput', () => {
  it('should create an instance', () => {
    const directive = new TextInput();
    expect(directive).toBeTruthy();
  });
});
