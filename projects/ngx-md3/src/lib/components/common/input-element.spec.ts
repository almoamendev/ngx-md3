import { InputElement } from './input-element';

describe('InputElement', () => {
  it('should create an instance', () => {
    const directive = new InputElement();
    expect(directive).toBeTruthy();
  });
});
