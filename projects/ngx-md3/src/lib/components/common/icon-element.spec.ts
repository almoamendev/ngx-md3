import { IconElement } from './icon-element';

describe('IconElement', () => {
  it('should create an instance', () => {
    const directive = new IconElement();
    expect(directive).toBeTruthy();
  });
});
