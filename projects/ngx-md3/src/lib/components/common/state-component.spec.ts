import { StateComponent } from './state-component';

describe('StateComponent', () => {
  it('should create an instance', () => {
    const directive = new StateComponent();
    expect(directive).toBeTruthy();
  });
});
