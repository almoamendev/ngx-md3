import { TypeLabel } from './type-label';

describe('TypeLabel', () => {
  it('should create an instance', () => {
    const directive = new TypeLabel();
    expect(directive).toBeTruthy();
  });
});
