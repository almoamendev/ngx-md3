import { AppBarLogo } from './app-bar-logo';

describe('AppBarLogo', () => {
  it('should create an instance', () => {
    const directive = new AppBarLogo();
    expect(directive).toBeTruthy();
  });
});
