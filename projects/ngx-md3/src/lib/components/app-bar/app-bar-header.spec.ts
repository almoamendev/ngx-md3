import { AppBarHeader } from './app-bar-header';

describe('AppBarHeader', () => {
  it('should create an instance', () => {
    const directive = new AppBarHeader();
    expect(directive).toBeTruthy();
  });
});
