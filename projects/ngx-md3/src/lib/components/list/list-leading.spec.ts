import { ListLeading } from './list-leading';

describe('ListLeading', () => {
  it('should create an instance', () => {
    const directive = new ListLeading();
    expect(directive).toBeTruthy();
  });
});
