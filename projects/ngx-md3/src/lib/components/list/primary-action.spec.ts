import { PrimaryAction } from './primary-action';

describe('PrimaryAction', () => {
  it('should create an instance', () => {
    const directive = new PrimaryAction();
    expect(directive).toBeTruthy();
  });
});
