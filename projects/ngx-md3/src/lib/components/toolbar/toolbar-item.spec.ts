import { ToolbarItem } from './toolbar-item';

describe('ToolbarItem', () => {
  it('should create an instance', () => {
    const directive = new ToolbarItem();
    expect(directive).toBeTruthy();
  });
});
